spring:
  application:
    name: configserver
    
  cloud:
    vault:
      #host: localhost # (default localhost)
      #port: 8200      # (default 8200)
      scheme: https
      authentication: token
      token: your_vault_token